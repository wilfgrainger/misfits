# D1 Migration 0006 Production Evidence

**Migration:** `migrations/0006_private_club_membership.sql`  
**Database:** `misfits`  
**Binding:** `DB`  
**Status:** EXECUTED AND VERIFIED  
**Date approved:** 23 August 2026  
**Production executor:** GitHub Actions only

## Purpose

This file is the durable production evidence record for private-club migration 0006.

Never paste Cloudflare credentials, Google credentials, session material or raw club invite tokens here.

## Approved immutable migration source

For this release, the approved migration source was:

```text
fd2f2bb0180d03828131bd700e48d5c6582cabff
```

The production migration workflow checked out this exact SHA and reran the full gate before touching D1.

## Migration controller evidence

The controller used for migration 0006 was already on `main`:

- ops PR: #173 `ops: add manual production D1 migration gate`;
- merged main SHA: `c58718b11cb89e0b04d62c0d11965ede5ba77ee4`;
- workflow file: `.github/workflows/manual-d1-migration.yml`;
- current-main display name at execution time: `Manual production D1 migration`;
- TDD RED: CI #801 failed exactly because the workflow file did not exist;
- TDD GREEN: CI #805 passed after the workflow and safety test were implemented.

The controller is `workflow_dispatch` only, requires an immutable 40-character SHA plus typed `APPLY-D1` confirmation, runs the full code gate before migration, verifies D1 afterwards, and never deploys application code.

PR #172 also makes the same workflow the durable authority for future production D1 changes:

- display name becomes `Production D1 management`;
- the one workflow remains the only normal production mutation path;
- it lists pending migrations before and after apply;
- it runs generic D1 integrity/schema verification after apply;
- it still has no Worker deploy command and no automatic trigger;
- RED CI #812 proved the durable workflow contract was not yet met;
- GREEN CI #813 passed the new contract with 63/63 test files and 258/258 tests, TypeScript, Impeccable and production build green.

## Approved GitHub Actions invocation

The production workflow was dispatched from `main` with:

```text
migration_sha = fd2f2bb0180d03828131bd700e48d5c6582cabff
confirmation  = APPLY-D1
```

There is no local production fallback. Authenticated local Wrangler is not an alternative executor for production D1 changes.

## Actual migration execution

**Status:** SUCCESS

**Execution method:** GitHub Actions  
**GitHub Actions run ID:** `32633942454`  
**GitHub Actions run link:** `https://github.com/wilfgrainger/misfits/actions/runs/32633942454`  
**Executed by/environment:** `wilfgrainger` / GitHub `production` environment  
**Execution date/time:** 23 August 2026, 10:30:35–10:31:23 UTC  
**Immutable migration SHA actually checked out:** `fd2f2bb0180d03828131bd700e48d5c6582cabff`  
**Pending migrations before apply:** `0006_private_club_membership.sql` only  
**Migration command result summary:** Wrangler executed 6 remote commands and reported `0006_private_club_membership.sql` successful.

### Pre-migration gate evidence

The same workflow reran the approved source before touching production D1:

- `npm ci`: success; 0 vulnerabilities reported;
- `npx wrangler types`: success;
- `npm run typecheck`: success;
- `npm test`: 62/62 test files and 257/257 tests passed at the immutable migration SHA;
- `npm run build`: success; 35 modules transformed;
- `git diff --check`: success;
- Cloudflare credential presence check: success.

## Required remote verification

The workflow executed:

```sql
PRAGMA table_info(users);
SELECT name FROM sqlite_master WHERE type='table' AND name='club_invites';
SELECT club_status, COUNT(*) FROM users GROUP BY club_status;
SELECT visibility, COUNT(*) FROM leagues GROUP BY visibility;
```

### `users.club_status`

**Status:** VERIFIED

The remote `users` schema includes `club_status` as `TEXT NOT NULL` with default `'PENDING'`, matching the migration contract.

### `club_invites`

**Status:** VERIFIED

The remote schema query returned the `club_invites` table.

### Club-status backfill

**Status:** VERIFIED

Actual grouped counts after migration:

```text
APPROVED = 1
PENDING  = 1
```

No unexpected status values were returned. Identity-level data was deliberately not written into release evidence.

### League privacy

**Status:** VERIFIED

Actual grouped counts after migration:

```text
PRIVATE = 1
```

No public league remained in the grouped result.

## Release decision

**Migration verified safe for application deploy:** YES.

Run `32633942454` completed successfully, the approved immutable SHA was used, migration 0006 applied successfully, and all four required remote verification areas returned plausible production state.

## Remaining release sequence

1. Ensure PR #172 has fresh green CI on the exact head containing this evidence.
2. Confirm PR #172 remains mergeable and mark ready if still draft.
3. Merge PR #172.
4. Verify the existing `main` workflow deploys the Worker successfully.
5. Run production health/auth/privacy smoke checks.
6. Update `PROGRESS.md` and the release handoff with merge SHA, deploy evidence and rendered-acceptance status.
