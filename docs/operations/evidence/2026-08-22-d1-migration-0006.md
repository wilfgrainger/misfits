# D1 Migration 0006 Production Evidence

**Migration:** `migrations/0006_private_club_membership.sql`  
**Database:** `misfits`  
**Binding:** `DB`  
**Status:** APPROVED BY USER, NOT YET EXECUTED  
**Date approved:** 23 August 2026  
**Preferred executor:** GitHub Actions `Manual production D1 migration`

## Purpose

This file is the durable production evidence record for private-club migration 0006. Complete it with real, non-secret execution results before PR #172 is merged.

Never paste Cloudflare credentials, Google credentials, session material or raw club invite tokens here.

## Approved immutable migration source

For the current release, the approved migration source is:

```text
fd2f2bb0180d03828131bd700e48d5c6582cabff
```

This feature head passed full CI #800 before the manual migration controller was introduced. The GitHub workflow checks out the exact SHA and reruns the full gate before touching D1.

Do not substitute a branch name or a newer SHA without re-establishing the relevant release evidence.

## Migration controller evidence

The manual controller is already on `main`:

- ops PR: #173 `ops: add manual production D1 migration gate`;
- merged main SHA: `c58718b11cb89e0b04d62c0d11965ede5ba77ee4`;
- workflow file: `.github/workflows/manual-d1-migration.yml`;
- TDD RED: CI #801 failed exactly because the workflow file did not exist;
- TDD GREEN: CI #805 passed after the workflow and safety test were implemented.

The controller is `workflow_dispatch` only, requires an immutable 40-character SHA plus typed `APPLY-D1` confirmation, runs the full code gate before migration, verifies D1 afterwards, and never deploys application code.

## Approved GitHub Actions invocation

Open the repository's Actions page and run **Manual production D1 migration** from `main` with:

```text
migration_sha = fd2f2bb0180d03828131bd700e48d5c6582cabff
confirmation  = APPLY-D1
```

The workflow performs:

```bash
npm ci
npx wrangler types
npm run typecheck
npm test
npm run build
git diff --check
npx wrangler d1 migrations list misfits --remote
npm run db:migrate:remote
```

Then it executes the required remote verification queries automatically.

### Local fallback

An authenticated local Wrangler environment may instead run:

```bash
npm ci
npm run db:migrate:remote
```

Use the same verification queries below and capture equivalent evidence.

## Cloudflare credential condition

The existing GitHub `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` are used. Their exact token scope is not recorded here.

If the workflow fails at the first remote D1 command because the token lacks D1 write/edit permission:

- migration remains **NOT EXECUTED**;
- do not merge PR #172;
- adjust only the minimum required Cloudflare permission under the repository's explicit secret/permission rules;
- rerun the same immutable migration SHA.

A credential failure is not migration evidence and must not be recorded as a successful release gate.

## Actual migration execution

**Status:** PENDING

**Execution method:** _GitHub Actions / local Wrangler_  
**GitHub Actions run ID:** _fill after execution_  
**GitHub Actions run link:** _fill after execution_  
**Executed by/environment:** _fill after execution_  
**Execution date/time:** _fill after execution_  
**Immutable migration SHA actually checked out:** _fill after execution_  
**Pending migrations before apply:** _fill after execution_  
**Migration command result summary:** _fill after execution_

## Required remote verification

```sql
PRAGMA table_info(users);
SELECT name FROM sqlite_master WHERE type='table' AND name='club_invites';
SELECT club_status, COUNT(*) FROM users GROUP BY club_status;
SELECT visibility, COUNT(*) FROM leagues GROUP BY visibility;
```

### `users.club_status`

**Status:** PENDING

Expected: column exists with the migration-defined permanent membership semantics.

Actual non-secret summary: _fill after execution_

### `club_invites`

**Status:** PENDING

Expected: table exists.

Actual non-secret summary: _fill after execution_

### Club-status backfill

**Status:** PENDING

Expected: existing admins/master admin and active league members are approved according to the migration contract; other historical identities follow the approved backfill rules.

Actual grouped counts: _fill after execution_

Unexpected rows investigated: _fill after execution_

### League privacy

**Status:** PENDING

Expected: existing leagues are `PRIVATE`.

Actual grouped counts: _fill after execution_

## Release decision

**Migration verified safe for application deploy:** NO, pending actual execution and verification.

Do not change this to YES merely because the workflow was dispatched. It becomes YES only when the migration job succeeds and all four verification areas are plausible.

## After successful verification

1. Fill this document with the actual non-secret run evidence.
2. Ensure PR #172 has fresh green CI on its resulting exact head.
3. Resolve any merge-base drift from `main` without changing the approved release contract.
4. Confirm PR #172 is mergeable and mark ready if still draft.
5. Merge PR #172.
6. Verify the existing `main` workflow deploys the Worker successfully.
7. Run production health/auth/privacy smoke checks.
8. Update `PROGRESS.md` and the release handoff with the migration run, merge SHA, deploy evidence and rendered-acceptance status.
